# Core scientific stack
numpy>=1.23
pandas>=1.5
scikit-learn>=1.1          # TF-IDF features + ROC/PR metrics
matplotlib>=3.6            # figures
 
# String kernels
rapidfuzz>=3.0            # fast normalized Levenshtein distance (rapidfuzz.process.cdist)
cvxopt>=1.3              # quadratic-program solver behind courselib's BinaryKernelSVM
 
# Note: `courselib` is vendored directly in this repository (it ships no setup.py),
# so it requires no separate installation -- it is importable as long as you run the
# scripts from the project root.